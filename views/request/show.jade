h1 请求列表
for item in requests
  div
    h2 买家
    p= item.buyeremail
    p.buyerid= item.buyerid
    h2 卖家
    p= item.selleremail
    p.sellerid= item.sellerid
    h2 书名
    p= item.bookname
    h2 数量
    p= item.count
    p.rid.hide= item._id
    if user.uid == item.buyerid
      a.reqdel(href='request/del') 删除
    if user.uid == item.sellerid
      a.reqconfirm(href='request/confiem') 确认交易
